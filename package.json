{
  "name": "school-client",
  "version": "1.0.0",
  "description": "school project",
  "scripts": {
    "init": "npm install lerna && lerna bootstrap --hoist "
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jiawang1/school.git"
  },
  "engines": {
    "node": ">= 8.0.0"
  },
  "devDependencies": {
    "babel-eslint": "^8.2.2"
  },
  "dependencies": {
    "lerna": "^2.11.0"
  }
}
