const PROJECT_TYPE = {
  application: 1,
  component: 2
};
const STATE_MANAGEMENT = {
  dva: 1,
  apollo: 2
};

const COMPONENT_TEMPLATE = 'template';

module.exports = { PROJECT_TYPE, STATE_MANAGEMENT, COMPONENT_TEMPLATE };
